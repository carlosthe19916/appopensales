'use strict';

// Directives for common buttons
angular.module(ApplicationConfiguration.applicationModuleName).controller('ServerInfoController', function (OSSession) {

  console.log(OSSession);

});
